// constants/radius.ts

export const RADIUS = {
  card: 18,      // main cards: Home, History cards, Stats cards
  innerCard: 16, // inner blocks: chart containers, inner panels
  button: 12,    // primary buttons
  badge: 16,     // small badges / icons / chips
};
